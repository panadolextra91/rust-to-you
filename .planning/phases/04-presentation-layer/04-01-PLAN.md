---
phase: 04-presentation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - src/lib.rs
  - src/i18n.rs
  - src/error.rs
  - src/app/collect.rs
  - src/tui/mod.rs
  - src/tui/format.rs
  - src/tui/section.rs
  - src/tui/report.rs
  - src/tui/plain.rs
autonomous: true
requirements: [PRES-01, PRES-02]
must_haves:
  truths:
    - "All user-facing text is narrated by Ferris (third-person) and bilingual VI+EN; Ferris NEVER says 'tôi/mình' in either language (D-08)"
    - "A pure, unit-tested i18n helper (Bilingual{vi,en} + two-line and inline render fns) is used by BOTH the TUI renderer and the plain renderer (D-08)"
    - "IntakeError Display emits a Ferris VI line then an EN line (no 'mình'); existing Phase 1 cli::parse variant tests still pass (D-08)"
    - "The degrade message in src/app/collect.rs is Ferris + bilingual (VI line then EN line) (D-08)"
    - "Header renders 🦀 rust-to-you, 'Repository Investigation Report', owner/repo, investigation date, and Case ID (D-07)"
    - "All 6 factual sections render in fixed top-to-bottom order from FactualSections (D-01, D-07)"
    - "Section titles + narrative render as two lines (VI then EN); dense data-row labels render bilingual inline (e.g. 'Tổng commit / Total commits: 12,442') with shared values (D-08)"
    - "Language Soup renders ASCII bars (██████░░░░ 62%); large numbers get thousands separators; missing Option data renders as '—' (D-04)"
    - "Relative dates render from a unix-epoch seconds timestamp with an injected now (D-04)"
    - "Sections render with color + expressive emoji while staying readable (D-05, PRES-02)"
    - "When stdout is NOT a TTY, a plain-text report with the same bilingual section labels/order is emitted (D-06, D-08)"
    - "Sections 7-9 (Vibes/Findings/Verdict) are NOT rendered and there are NO placeholders for them (D-01)"
  artifacts:
    - path: "src/i18n.rs"
      provides: "Pure Bilingual{vi,en} type + two_line(b)->[VI line, EN line] + inline_label(b)->'vi / en' render helpers, shared by TUI + plain (D-08)"
      contains: "pub struct Bilingual"
    - path: "src/tui/format.rs"
      provides: "Pure format helpers ascii_bar, thousands, dash_or, relative_date (D-04)"
      contains: "pub fn ascii_bar"
    - path: "src/tui/section.rs"
      provides: "Reusable Section abstraction: title line + body lines + into_lines() (D-01)"
      contains: "pub struct Section"
    - path: "src/tui/report.rs"
      provides: "build_report_lines(): header + 6 sections -> Vec<Line>, bilingual via i18n (PRES-01, D-08)"
      contains: "pub fn build_report_lines"
    - path: "src/tui/plain.rs"
      provides: "Non-TTY plain-text renderer writing to impl Write, bilingual via i18n (D-06, D-08)"
      contains: "pub fn render"
    - path: "src/error.rs"
      provides: "IntakeError Display retrofitted to Ferris + bilingual VI/EN, no 'mình' (D-08)"
      contains: "Ferris"
    - path: "src/tui/mod.rs"
      provides: "tui module root re-exporting submodules"
      contains: "pub mod format"
  key_links:
    - from: "src/tui/report.rs"
      to: "src/i18n.rs"
      via: "calls two_line/inline_label to build bilingual title + label strings"
      pattern: "i18n::(two_line|inline_label)|crate::i18n"
    - from: "src/tui/plain.rs"
      to: "src/i18n.rs"
      via: "calls the same i18n render helpers so piped output matches the TUI"
      pattern: "i18n::(two_line|inline_label)|crate::i18n"
    - from: "src/tui/report.rs"
      to: "src/report/sections.rs (FactualSections)"
      via: "borrow &FactualSections, render only, recompute nothing"
      pattern: "build_report_lines.*FactualSections"
    - from: "src/tui/report.rs"
      to: "src/tui/format.rs"
      via: "calls ascii_bar/thousands/dash_or/relative_date to build strings"
      pattern: "format::(ascii_bar|thousands|dash_or|relative_date)"
    - from: "src/lib.rs"
      to: "src/i18n.rs and src/tui/mod.rs"
      via: "pub mod i18n; pub mod tui;"
      pattern: "pub mod (i18n|tui)"
---

<objective>
Build the report renderer foundation for Phase 4: add the two new deps (ratatui 0.30, crossterm 0.29), create the centralized bilingual i18n helper (D-08), create the `src/tui/` module, and produce the pure format helpers, the reusable Section abstraction (D-01), `build_report_lines` for the header + 6 factual sections (PRES-01/D-07) with bilingual Ferris-narrated text (D-08), and the non-TTY plain-text fallback (D-06). This plan also retrofits the Phase 1-3 Vietnamese-only strings (`IntakeError` Display + the `collect.rs` degrade message) to Ferris + bilingual (D-08). This is Wave 0 of the validation strategy: it creates every test scaffold the consumers in 04-02 depend on.

Purpose: Turn Phase 3's pure `FactualSections` view model into renderable `Vec<Line>` and a parallel plain-text report, all narrated by Ferris in VI+EN through ONE centralized, unit-tested i18n helper, with formatting unit-tested and the render verified headlessly via `ratatui::backend::TestBackend`. No interactive loop, no terminal lifecycle yet — that is 04-02.

Output: `src/i18n.rs`, `src/tui/{mod,format,section,report,plain}.rs`, retrofitted `src/error.rs` + `src/app/collect.rs`, `pub mod i18n;` + `pub mod tui;` in `src/lib.rs`, ratatui/crossterm in Cargo.toml, and passing `cargo test --lib i18n`, `tui::format`, `tui::section`, `tui::report`, `tui::plain`, plus the existing `cli::parse` suite still green.
</objective>

<execution_context>
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-presentation-layer/04-CONTEXT.md
@.planning/phases/04-presentation-layer/04-RESEARCH.md
@.planning/phases/04-presentation-layer/04-VALIDATION.md
@src/report/sections.rs
@src/app/session.rs
@src/app/run.rs
@src/error.rs
@src/app/collect.rs
@src/cli/parse.rs
@src/lib.rs
</context>

<artifacts_this_phase_produces>
This plan (04-01) produces:
- New deps: `ratatui@0.30`, `crossterm@0.29` (only two; thousands-sep + relative-date hand-rolled from chrono — already a dep).
- `src/i18n.rs` (top-level, `pub mod i18n;` in lib.rs): the centralized D-08 bilingual helper — `pub struct Bilingual { pub vi: String, pub en: String }` (with a `Bilingual::new(vi, en)` or `bi(vi, en)` constructor) + two pure render helpers: `two_line(&Bilingual) -> [String; 2]` (VI line, then EN line) and `inline_label(&Bilingual) -> String` (`"{vi} / {en}"`). Pure, no terminal types, unit-tested.
- New deps registered: `ratatui@0.30`, `crossterm@0.29` (only two; thousands-sep + relative-date hand-rolled from chrono — already a dep).
- Module `src/tui/` with `pub mod tui;` registered in `src/lib.rs`.
- `src/tui/format.rs`: pure helpers `ascii_bar(fraction, width) -> String`, `thousands(u64) -> String`, `dash_or<T>(Option<T>, impl FnOnce(T)->String) -> String`, `relative_date(now_secs, then_secs) -> String`.
- `src/tui/section.rs`: `Section { title: Line<'static>, body: Vec<Line<'static>> }` + `into_lines()` (D-01 reusable abstraction; Phase 5 reuses it verbatim for Sections 7-9).
- `src/tui/report.rs`: `build_report_lines(session, sections) -> Vec<Line<'static>>` — header (D-07) + the 6 factual section builders, all bilingual via i18n (D-08).
- `src/tui/plain.rs`: `render(w: impl Write, session, sections) -> io::Result<()>` non-TTY plain text (D-06), bilingual via the SAME i18n helper (D-08).
- Retrofit (D-08): `src/error.rs` `IntakeError` Display → Ferris + bilingual; `src/app/collect.rs` degrade message → Ferris + bilingual.

Deferred to 04-02 (do NOT build here): `app.rs` (TuiState/event loop/scroll), terminal lifecycle, the `IsTerminal` branch, wiring `app::run` -> `tui::render`.
Deferred to Phase 5 (do NOT build here): Sections 7-9 logic OR rendering — NO placeholders.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Centralized bilingual i18n helper (D-08) + retrofit Phase 1-3 strings</name>
  <files>src/i18n.rs, src/lib.rs, src/error.rs, src/app/collect.rs</files>
  <read_first>
    - src/lib.rs (the `pub mod` list — add `pub mod i18n;` near the top, before `pub mod app;` so error.rs/collect.rs can use it)
    - src/error.rs (the 8 `IntakeError` variants whose `#[error(...)]` Display strings currently say "mình"/"Bạn" and are Vietnamese-only — to retrofit to Ferris + bilingual)
    - src/app/collect.rs (the `eprintln!("🦀 Lỗi khi gọi metadata: {:?}. Continuing git-only...", e)` degrade message — to retrofit to Ferris + bilingual)
    - src/cli/parse.rs § `#[cfg(test)] mod tests` (CRITICAL: the parse tests assert `res.unwrap_err() == expected_err` comparing IntakeError ENUM VARIANTS via PartialEq — NOT Display strings; confirmed safe to change Display text. Re-verify before editing.)
    - src/main.rs (the two `eprintln!("{}", e)` sites — the Display impl must therefore emit BOTH the VI line and the EN line, newline-separated, so a single `{}` prints both lines)
    - .planning/phases/04-presentation-layer/04-CONTEXT.md D-08 (Ferris third-person voice in BOTH languages; never "tôi/mình"; errors/status/titles/narrative = two lines VI then EN; data-row labels = inline "vi / en")
  </read_first>
  <action>
    Create `src/i18n.rs` as a PURE, top-level module (no ratatui/crossterm/terminal types — it is consumed by error.rs which must not depend on tui). Define `pub struct Bilingual { pub vi: String, pub en: String }` with a convenience constructor `pub fn bi(vi: impl Into<String>, en: impl Into<String>) -> Bilingual` (free fn) or `Bilingual::new`. Add two pure render helpers: `pub fn two_line(b: &Bilingual) -> [String; 2]` returning `[b.vi.clone(), b.en.clone()]` (Vietnamese line first, English line second — used for errors, status messages, section titles, and narrative per D-08); and `pub fn inline_label(b: &Bilingual) -> String` returning `format!("{} / {}", b.vi, b.en)` (used for dense data-row labels per D-08; the shared value/number is appended by the caller, e.g. `format!("{}: {}", inline_label(&label), value)`). Keep all of this terminal-agnostic so BOTH the TUI renderer (Spans) and the plain renderer reuse it.
    Add `pub mod i18n;` to `src/lib.rs` before `pub mod app;`.
    Retrofit `src/error.rs`: rewrite every `IntakeError` variant's `#[error(...)]` Display so it (a) uses Ferris in third person — NEVER "mình"/"tôi"/"Bạn" referring to Ferris — and (b) emits a Vietnamese line, a newline, then an English line, so the existing `eprintln!("{}", e)` in main.rs prints both lines. Keep all 🦀 + the existing structured fields (`{host}`, `{owner}`, `{repo}`, `{input}`, `{detail}`). Example for `UnsupportedHost`: VI `🦀 Ferris chỉ hóng repo GitHub thôi — {host} chưa có trong danh sách khách mời` then a newline then EN `Ferris can only visit public GitHub repos`. Apply the same VI-line/EN-line pattern to all 8 variants (EmptyInput, NotAUrl, UnsupportedHost, MalformedRepoPath, RepoNotFoundOrPrivate, Network, RateLimited, CollectionFailed). Use Ferris's playful gossip tone in both languages (English copy is Claude's discretion). Do NOT change the enum variants, fields, derives, or `exit_code()` — only the Display strings. (The thiserror `#[error("...\n...")]` literal with an embedded `\n` produces the two-line Display.)
    Retrofit `src/app/collect.rs`: change the degrade `eprintln!` so it is Ferris + bilingual — a Vietnamese line then an English line (e.g. VI `🦀 Ferris không gọi được metadata ({:?}) — tiếp tục với git thôi` then EN `Ferris could not reach repo metadata, continuing git-only`). Keep the `{:?}` debug of the underlying error in the VI line (or drop it from EN). Do not change control flow — it still falls through to `RepoMetaState::Unavailable`. (You MAY route this string through `i18n::two_line` + two `eprintln!`s, or inline a single `eprintln!` with an embedded `\n`; either satisfies D-08 — keep it simple.)
    Add `#[cfg(test)] mod tests` to `src/i18n.rs`: `two_line(&bi("vi", "en")) == ["vi".to_string(), "en".to_string()]` (VI first, EN second); `inline_label(&bi("Tổng commit", "Total commits")) == "Tổng commit / Total commits"`; and a guard that `two_line` preserves order (index 0 is the VI string, index 1 is the EN string).
    Do NOT place fenced code blocks in implementation prose — write the actual Rust in the files via the editor.
  </action>
  <verify>
    <automated>cargo test --lib i18n && cargo test --lib cli::parse</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib i18n` passes: `two_line(&bi("vi","en")) == ["vi","en"]` (VI first), `inline_label(&bi("Tổng commit","Total commits")) == "Tổng commit / Total commits"`.
    - `cargo test --lib cli::parse` STILL passes (variant-equality assertions unaffected by Display retrofit) — proves D-08 retrofit did not break Phase 1.
    - `src/error.rs`: every variant's Display contains "Ferris" and the rendered Display contains a VI substring AND an EN substring; NO occurrence of "mình" anywhere in the Display strings (`grep -v '^#' src/error.rs | grep -c 'mình'` returns 0).
    - `src/app/collect.rs` degrade message contains "Ferris" and both a VI and an EN line; no "mình".
    - `src/lib.rs` contains `pub mod i18n;`; `src/i18n.rs` is pure (no ratatui/crossterm/terminal imports).
  </acceptance_criteria>
  <done>The centralized Bilingual i18n helper exists, is pure and unit-tested; IntakeError Display + the collect.rs degrade message are retrofitted to Ferris + bilingual (no "mình"); existing cli::parse variant tests still pass (D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Add deps, create tui module, and the pure format helpers with tests</name>
  <files>Cargo.toml, Cargo.lock, src/lib.rs, src/tui/mod.rs, src/tui/format.rs</files>
  <read_first>
    - src/lib.rs (the `pub mod` list — add `pub mod tui;` after `pub mod report;`; `pub mod i18n;` already added in Task 1)
    - .planning/phases/04-presentation-layer/04-RESEARCH.md § "Standard Stack" (exact dep versions + `cargo add --dry-run` verification) and § "Pure format helpers" (reference signatures for ascii_bar/thousands/dash_or/relative_date)
    - .planning/phases/04-presentation-layer/04-CONTEXT.md D-04 (formatting rules: bars, relative dates, missing→"—", thousands separators)
    - Cargo.toml (confirm chrono is already present — do NOT re-add; confirm ratatui/crossterm are absent before adding)
  </read_first>
  <action>
    Run `cargo add ratatui@0.30 --dry-run` and `cargo add crossterm@0.29 --dry-run` first to confirm patch resolution, then `cargo add ratatui@0.30` and `cargo add crossterm@0.29`. After adding, run `cargo tree -i crossterm` and confirm a single crossterm version (ratatui 0.30 pulls crossterm 0.29 — assumption A3); if a duplicate appears, align versions before proceeding.
    Add `pub mod tui;` to `src/lib.rs` after `pub mod report;`.
    Create `src/tui/mod.rs` declaring the submodules for this plan: `pub mod format;`, `pub mod section;`, `pub mod report;`, `pub mod plain;`. (Do NOT declare `app` — that is 04-02. Do NOT add the `render()` entry fn yet — that is 04-02.)
    Create `src/tui/format.rs` with four pure helpers (no terminal types, return `String`): `ascii_bar(fraction: f64, width: usize) -> String` filling with `█` (U+2588) and padding with `░` (U+2591), clamping fraction to 0.0..=1.0 and rounding filled count; `thousands(n: u64) -> String` inserting `,` every 3 digits from the right; `dash_or<T>(opt: Option<T>, f: impl FnOnce(T) -> String) -> String` returning the em-dash `"—"` (U+2014) on None per D-04; `relative_date(now_secs: i64, then_secs: i64) -> String` with branches just-now / min ago / hours ago / days ago / months ago / years ago using injected `now_secs` (the value is a unix-epoch seconds timestamp per the render contract).
    Add a `#[cfg(test)] mod tests` with assertions matching the validation map: `ascii_bar(0.62, 10) == "██████░░░░"` (6 filled), boundary cases `ascii_bar(0.0,10)` all-empty and `ascii_bar(1.0,10)` all-filled; `thousands(12442) == "12,442"` and `thousands(0) == "0"` and `thousands(999) == "999"`; `dash_or::<u64>(None, |n| n.to_string()) == "—"` and the Some case formats; `relative_date` boundary cases with an injected now proving "3 days ago", a months bucket, and a years bucket.
    Do NOT place fenced code blocks in implementation prose — write the actual Rust in the files via the editor. Keep styling out of these helpers (they return plain Strings; color is applied later at the Span site per Pitfall 4).
  </action>
  <verify>
    <automated>cargo test --lib tui::format</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds, proving ratatui/crossterm link.
    - `cargo tree -i crossterm` shows a single crossterm 0.29.x.
    - `cargo test --lib tui::format` passes, including `ascii_bar(0.62,10)=="██████░░░░"`, `thousands(12442)=="12,442"`, `dash_or(None)=="—"`, and the three relative_date buckets with injected now.
    - `src/lib.rs` contains `pub mod tui;`.
    - Cargo.toml lists exactly two new deps (ratatui, crossterm); no other new deps added.
  </acceptance_criteria>
  <done>format.rs helpers exist, are pure (no terminal types), and all format unit tests pass; deps added and linking.</done>
</task>

<task type="auto">
  <name>Task 3: Section abstraction + build_report_lines (header + 6 sections, bilingual) with TestBackend tests</name>
  <files>src/tui/section.rs, src/tui/report.rs</files>
  <read_first>
    - src/report/sections.rs (the EXACT render input: `FactualSections` + `FirstImpressions` + the 6 section structs and their exact field names — already verified: FirstImpressions{repo_age_days,default_branch,stars,forks,contributor_count,last_activity_secs}; CommitCrimes{total_commits,commits_this_month,top_contributor,bus_factor,top_author_share_pct}; BranchJungle{total,active,stale,oldest_branch}; AncientRelics{oldest_file,most_modified_file,oldest_contributor,longest_living_branch}; LanguageSoup{languages:Vec<LanguageEntry{name,pct}>}; InfrastructureFootprints{8 bools})
    - src/app/session.rs (`InvestigationSession{repo:RepoRef{owner,repo}, case_id, started_at:SystemTime}` — header source for D-07)
    - src/i18n.rs (the Bilingual type + `two_line`/`inline_label`/`bi` from Task 1 — section titles + narrative use `two_line`; data-row labels use `inline_label`)
    - .planning/phases/04-presentation-layer/04-RESEARCH.md § "Pattern 3: Reusable Section abstraction", § "Pattern 4: Styled content with emoji", § "Render Input Contract", § "Pitfall 1/2" (emoji width + bar alignment), § "TestBackend smoke test"
    - .planning/phases/04-presentation-layer/04-CONTEXT.md D-01 (6 sections only, no placeholders for 7-9), D-05 (emoji/color), D-07 (header fields + fixed order), D-08 (bilingual: titles/narrative two-line VI then EN; data labels inline "vi / en"; Ferris third-person)
    - src/tui/format.rs (the helpers from Task 2 to call when building strings)
  </read_first>
  <action>
    Create `src/tui/section.rs`: `pub struct Section { pub title: Line<'static>, pub body: Vec<Line<'static>> }` with `pub fn into_lines(self) -> Vec<Line<'static>>` that pushes the title, extends with body, and pushes one blank `Line::default()` spacer. This is the D-01 reusable abstraction — keep it free of any section-specific knowledge so Phase 5 can build Sections 7-9 the same way. NOTE: a Section's `title` is a single `Line`; for the D-08 two-line bilingual title (VI then EN), the SECTION BUILDERS in report.rs emit the EN line as the first body line (i.e. title = VI line, body[0] = EN line) so the two-line title block renders correctly through `into_lines()`. Keep `Section` itself bilingual-agnostic.
    Create `src/tui/report.rs` with `pub fn build_report_lines(session: &InvestigationSession, sections: &FactualSections) -> Vec<Line<'static>>`. Borrow inputs read-only; recompute NOTHING (ARCHITECTURE Anti-Pattern 1). Internally:
    - `header_lines(session)`: build D-07 identity — a 🦀 rust-to-you title line, a "Repository Investigation Report" line, an `owner/repo` line, an investigation-date line (format `session.started_at` via chrono), and a `Case {case_id}` line. Apply color/Modifier::BOLD at the Span site, not in helpers. The "Repository Investigation Report" subtitle is bilingual two-line via `i18n::two_line(&bi("Báo cáo Điều tra Repository", "Repository Investigation Report"))` (VI line then EN line) per D-08; the 🦀 rust-to-you / owner/repo / date / Case ID lines are identifiers and stay single-line.
    - Six section builder fns returning `Section`, in fixed order: `first_impressions_section`, `commit_crimes_section`, `branch_jungle_section`, `ancient_relics_section`, `language_soup_section`, `infrastructure_section`. Each builds a STYLED BILINGUAL TITLE via `i18n::two_line` (D-08): title `Line` = the Vietnamese title with its emoji (D-05; e.g. 🦀/☠️/🔥/🌿/🏺/⚙️ — implementer's taste, keep readable), and the EN title line as the first body line. Narrative/status lines also use `two_line` (VI then EN). DATA-ROW labels use `i18n::inline_label` so the row reads `{vi} / {en}: {value}` with the value shared (e.g. `format!("{}: {}", inline_label(&bi("Tổng commit","Total commits")), thousands(total_commits))`). Use `format::dash_or` for every Option field (stars/forks/top_contributor/oldest_branch/all four relics/last_activity), `format::thousands` for large counts (total_commits, stars, forks), `format::relative_date` for `last_activity_secs` (pass a `now_secs`; accept it as a param or use `chrono::Utc::now().timestamp()` — but in tests inject a fixed now), `format::ascii_bar` for each LanguageEntry (build the bar from pct; empty languages vec → a single "—" line). Infrastructure: render the 8 bools as ✓/✗ accents, NEVER "—"; the infra row labels are bilingual inline. Ferris narration stays third-person (never "tôi/mình").
    - CRITICAL bar alignment (Pitfall 2): keep emoji OUT of the bar column. Build each language row as left-padded name (pad by display width) + space + bar + ` {pct}%`. Emoji live only in titles/labels, not in the aligned bar segment.
    - Render ONLY the 6 factual sections. Do NOT emit any Section 7/8/9 or placeholder for them (D-01).
    Add `#[cfg(test)] mod tests` with synthetic `FactualSections` + a test `InvestigationSession` (construct structs directly — see make_test_snapshot pattern in src/report/sections.rs; no network). Use `ratatui::backend::TestBackend` + `Terminal::new`, draw a `Paragraph::new(build_report_lines(..))`, then scan the buffer cells. Assert: header buffer contains "Repository Investigation Report" (EN) AND a Vietnamese substring (e.g. "Báo cáo") proving the bilingual two-line subtitle; the test case_id; the 6 section titles appear in fixed order (assert the row index of each title is monotonically increasing); at least one data row shows the bilingual inline `vi / en` pattern (assert a " / " separator appears on a label row); a language bar row contains the `█`/`░` bar cells aligned (assert specific cells/row); and that no Vibes/Findings/Verdict label appears (negative assertion guarding D-01). No occurrence of "mình" in the rendered buffer (D-08).
  </action>
  <verify>
    <automated>cargo test --lib tui::report tui::section</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib tui::section` passes (into_lines yields title + body + spacer).
    - `cargo test --lib tui::report` passes including: header substring "Repository Investigation Report" (EN) AND a Vietnamese subtitle substring present (bilingual two-line, D-08); the rendered case_id present; the 6 section titles present in fixed top-to-bottom order; at least one bilingual inline data label (`vi / en`) present; a language ASCII-bar row renders aligned `█░` cells; negative assertion confirms no Section 7-9 label is rendered; no "mình" in the rendered buffer.
    - No metric is recomputed in report.rs (only field reads from `&FactualSections`).
  </acceptance_criteria>
  <done>Section abstraction + build_report_lines render the bilingual header (D-07/D-08) and all 6 sections (D-01) with two-line titles/narrative + inline data labels, verified headlessly via TestBackend; bar alignment asserted; Sections 7-9 absent.</done>
</task>

<task type="auto">
  <name>Task 4: Non-TTY plain-text renderer (bilingual) with tests</name>
  <files>src/tui/plain.rs</files>
  <read_first>
    - src/app/run.rs (the current inline println! printer — the field-access pattern seed for the plain renderer; note it currently reads snapshot fields directly)
    - src/report/sections.rs (FactualSections — the plain renderer consumes the SAME view model as report.rs for consistency)
    - src/app/session.rs (header fields for the plain header)
    - src/i18n.rs (the SAME `two_line`/`inline_label`/`bi` helpers used by report.rs — reuse them so piped output matches the TUI exactly, D-08)
    - .planning/phases/04-presentation-layer/04-RESEARCH.md § "Non-TTY detection + branch (D-06)", § "Open Questions Q2" (recommendation: fresh plain renderer mirroring TUI section order/labels), § "Code Examples"
    - .planning/phases/04-presentation-layer/04-CONTEXT.md D-06 (non-TTY → plain text so `| less` works), D-08 (bilingual: titles/narrative two-line, data labels inline)
    - src/tui/format.rs (reuse the same helpers for consistency between piped and TUI output)
  </read_first>
  <action>
    Create `src/tui/plain.rs` with `pub fn render<W: std::io::Write>(w: &mut W, session: &InvestigationSession, sections: &FactualSections) -> std::io::Result<()>`. Write a plain-text report (no ANSI escape codes, no ratatui types) that mirrors the TUI section order and bilingual labels for consistency between piped and interactive output (per RESEARCH Open Question 2 recommendation — fresh plain layout, not a copy of run.rs's Vietnamese strings). Reuse `format::{dash_or, thousands, relative_date, ascii_bar}` AND `i18n::{two_line, inline_label, bi}` so values AND bilingual text match the TUI exactly (D-08). Emit: header (🦀 rust-to-you / the two-line "Báo cáo..." + "Repository Investigation Report" subtitle / owner/repo / date / Case ID), then the 6 sections in fixed order. For each section, write the two-line bilingual title (VI line then EN line via `two_line`) and the data rows with bilingual inline labels (`inline_label` → `vi / en: value`). Ferris narration stays third-person (never "tôi/mình"). Write via the generic `W` (so tests capture into a `Vec<u8>`); do NOT hard-code `println!`/stdout inside this fn.
    Accept a `now_secs` parameter (or thread the same now used by report.rs) for deterministic relative_date in tests.
    Add `#[cfg(test)] mod tests`: build synthetic `FactualSections` + test session, render into a `Vec<u8>`, convert to String, and assert the output contains each of the 6 sections' EN labels AND a Vietnamese substring (bilingual), the case_id, and the "Repository Investigation Report" title, and at least one inline `vi / en` data label. Assert there are no ANSI escape bytes (no `\x1b[`) and no "mình" (D-08).
  </action>
  <verify>
    <automated>cargo test --lib tui::plain</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib tui::plain` passes: captured output contains all 6 section labels (EN) plus a Vietnamese substring (bilingual, D-08), the case_id, the report title, and at least one inline `vi / en` data label.
    - Output contains no `\x1b[` escape sequences (pipe-safe) and no "mình".
    - `render` writes to a generic `impl Write` (test captures into `Vec<u8>`), not directly to stdout.
    - Plain renderer reuses `crate::i18n` helpers (grep `i18n` in src/tui/plain.rs matches) so piped + TUI text are identical.
  </acceptance_criteria>
  <done>plain::render emits a pipe-safe, bilingual plain-text report mirroring the TUI's section order/labels (D-06/D-08) via the shared i18n helper, verified by capturing into a Vec<u8>.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-derived strings → renderer | Branch/file/contributor names + repo description (already validated in Phase 1) flow into Spans (TUI) and plain text (non-TTY). |
| process → terminal/pipe | Rendered output is written to a TTY (Task 3 path, exercised in 04-02) or a pipe (Task 4 plain path); error/degrade text goes to stderr. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | repo strings rendered into TUI report (report.rs) | mitigate | Render via ratatui Span/Buffer/Cell model (Task 3) — content is written as cells, not raw bytes; never `print!` raw repo strings to the screen. |
| T-04-02 | Tampering | repo strings emitted to a pipe (plain.rs) | accept | Data fields are names/paths/booleans (low risk); `\| less` use case is acceptable. Plain renderer emits no raw escape bytes (asserted: no `\x1b[`). |
| T-04-03 | Denial of availability | gigantic Vec<Line> on a huge repo | accept | Render input is bounded (6 sections, short language list, integer branch counts — not per-branch lines). No unbounded growth. |
| T-04-08 | Tampering | bilingual i18n strings rendered into report/error/stderr | accept | i18n helper is pure string concatenation of static developer-authored copy + already-validated structured fields; no new untrusted input introduced by D-08. Display retrofit changes only literals. |
| T-04-SC | Tampering | cargo dependency installs (ratatui, crossterm) | mitigate | Both packages are canonical, named in STACK.md, corroborated by official docs.rs (Package Legitimacy Audit in RESEARCH = Approved, no [ASSUMED]/[SUS]). Confirm single crossterm version via `cargo tree -i crossterm`. |
</threat_model>

<verification>
- `cargo test --lib i18n` green (Bilingual + two_line + inline_label).
- `cargo test --lib cli::parse` green (Display retrofit did NOT break Phase 1 variant tests — D-08 safety gate).
- `cargo test --lib tui::` green (format + section + report + plain).
- `cargo build` clean (deps link).
- `cargo clippy` clean.
- `cargo tree -i crossterm` shows one crossterm 0.29.x.
- `grep -v '^#' src/error.rs | grep -c 'mình'` returns 0; "Ferris" present in error.rs and collect.rs (D-08 retrofit).
- No Section 7-9 rendering or placeholder anywhere (D-01) — enforced by the negative TestBackend assertion in Task 3.
</verification>

<success_criteria>
- Centralized pure i18n helper (Bilingual + two_line + inline_label) exists, is unit-tested, and is the SINGLE source of bilingual text for both renderers (D-08).
- IntakeError Display + the collect.rs degrade message are Ferris + bilingual (no "mình"); existing cli::parse tests still pass (D-08).
- format/section/report/plain modules exist with passing unit + TestBackend tests.
- Header (D-07) + 6 factual sections (D-01) render in fixed order, bilingual (two-line titles/narrative + inline data labels, D-08), with ASCII bars, thousands separators, relative dates, missing→"—", and expressive emoji/color (D-04/D-05).
- Non-TTY plain renderer mirrors section order/bilingual labels via the shared i18n helper and is pipe-safe (D-06/D-08).
- Only ratatui@0.30 + crossterm@0.29 added; chrono reused.
- `pub mod i18n;` + `pub mod tui;` registered in lib.rs.
- Sections 7-9 NOT rendered, no placeholders (D-01).
</success_criteria>

<output>
Create `.planning/phases/04-presentation-layer/04-01-SUMMARY.md` when done.
</output>
