---
phase: 05-polish-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/snapshot.rs
  - src/repo/branches.rs
  - src/app/collect.rs
  - src/analyze/mod.rs
  - src/analyze/vibes.rs
  - src/analyze/findings.rs
  - src/analyze/verdict.rs
  - src/tui/report.rs
  - src/tui/plain.rs
autonomous: true
requirements: [NARR-01, NARR-02]
must_haves:
  truths:
    - "Repository Vibes (Section 7) shows a single primary vibe label with evidence bullets that are the exact conditions that fired (NARR-01, D-02)"
    - "A low-signal repo falls through to Chaotic Good when no vibe scores >= MIN_SCORE=4 (D-02)"
    - "Interesting Findings (Section 8) shows ranked, capped (~4-6) bilingual observations plus a runner-up-vibe line (NARR-02, D-03)"
    - "Crab Verdict (Section 9) shows Strengths (checkmark), Risks (warn) and a one-line overall verdict label, bilingual + Ferris (NARR-02, D-04)"
    - "Sections 7, 8, 9 render after the 6 factual sections in fixed order 1->9 in BOTH the TUI buffer and the plain renderer (D-06)"
    - "release_tag_count is present on the snapshot, populated from git2 tag_names() (D-01)"
  artifacts:
    - path: "src/analyze/vibes.rs"
      provides: "VibeLabel enum (7 vibes), VibeResult{primary,primary_score,evidence,runner_up}, MIN_SCORE const, classify_vibes() pure fn over snapshot/FactualSections"
      contains: "MIN_SCORE"
    - path: "src/analyze/findings.rs"
      provides: "Finding struct + interesting_findings() pure fn (ranked, capped, runner-up line)"
      contains: "fn interesting_findings"
    - path: "src/analyze/verdict.rs"
      provides: "CrabVerdict{strengths,risks,overall_label} + crab_verdict() pure fn"
      contains: "fn crab_verdict"
    - path: "src/snapshot.rs"
      provides: "release_tag_count field on HistoryFacts or InvestigationSnapshot"
      contains: "release_tag_count"
    - path: "src/tui/report.rs"
      provides: "Sections 7-9 rendered via Section abstraction; updated negative assert (now PRESENT)"
      contains: "REPOSITORY VIBES"
    - path: "src/tui/plain.rs"
      provides: "Sections 7-9 in plain renderer"
      contains: "REPOSITORY VIBES"
  key_links:
    - from: "src/app/collect.rs"
      to: "release_tag_count via git2 tag_names()"
      via: "enrich-the-collector"
      pattern: "tag_names"
    - from: "src/tui/report.rs"
      to: "classify_vibes / interesting_findings / crab_verdict"
      via: "build_report_lines appends sections 7-9"
      pattern: "classify_vibes"
    - from: "src/analyze/findings.rs"
      to: "VibeResult.runner_up"
      via: "runner-up vibe becomes one finding line"
      pattern: "runner_up"
---

<objective>
Implement the three narrative analyzers — Repository Vibes (Section 7, per research/VIBES.md), Interesting Findings (Section 8), Crab Verdict (Section 9) — as pure functions over the snapshot/FactualSections, enrich the snapshot with `release_tag_count` (the only missing VIBES signal), and render sections 7-9 via the Phase 4 `Section` abstraction after the 6 factual sections in BOTH the TUI and plain renderers. All narrative text is bilingual VI+EN with the Ferris voice (D-08).

Purpose: Deliver NARR-01 (vibes from signals + evidence bullets) and NARR-02 (findings + crab verdict with strengths/risks). This is the narrative soul of the report; per Pitfall 3 every line must trace to a real signal.

Output: `release_tag_count` snapshot field; `src/analyze/{vibes,findings,verdict}.rs`; `VibeResult`/`Finding`/`CrabVerdict` types; sections 7-9 in `report.rs` + `plain.rs`; the Phase 4 negative assert UPDATED to assert presence.
</objective>

<execution_context>
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-calibration/05-CONTEXT.md
@.planning/research/VIBES.md
@.planning/research/PITFALLS.md
@.planning/research/METRICS.md
@src/snapshot.rs
@src/i18n.rs
@src/report/sections.rs
@src/analyze/commit.rs
@src/analyze/branch.rs
@src/analyze/infra.rs
@src/tui/section.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich snapshot with release_tag_count (D-01)</name>
  <read_first>
    - .planning/phases/05-polish-calibration/05-CONTEXT.md (D-01 — enrich-the-collector pattern, no network)
    - .planning/research/VIBES.md (`release_tag_count` signal row)
    - src/snapshot.rs (HistoryFacts struct — where to add the field)
    - src/app/collect.rs (collect() — HistoryFacts assembly at lines 46-61; uses `ws.repo: git2::Repository`)
    - src/repo/branches.rs + src/repo/history.rs (existing collector patterns; how git2 fns return Result<_, git2::Error> and are mapped to IntakeError::CollectionFailed)
  </read_first>
  <files>src/repo/branches.rs, src/snapshot.rs, src/app/collect.rs</files>
  <action>
    Add a `release_tag_count: usize` field to `HistoryFacts` in src/snapshot.rs (place it alongside the other history counts). Add a pure collector fn `release_tag_count(repo: &git2::Repository) -> Result<usize, git2::Error>` (place it in src/repo/branches.rs near `enumerate_branches`, or a new fn in history.rs — choose by cohesion) that calls git2 `repo.tag_names(None)` and returns the count of entries (`.iter().flatten().count()` style; tags are local git objects, NO network call). In src/app/collect.rs call this collector after the other history collectors, mapping its error to `IntakeError::CollectionFailed { detail: e.to_string() }` exactly like the neighbours, and set `release_tag_count` on the `HistoryFacts` literal. Update ALL existing `HistoryFacts { ... }` literals in test modules (src/report/sections.rs, src/analyze/commit.rs, src/analyze/branch.rs, src/analyze/infra.rs) to include the new field so the crate compiles. No new dependency; this is the same enrich pattern Phase 3 used.
  </action>
  <acceptance_criteria>
    - `cargo build --lib` compiles (all HistoryFacts literals updated).
    - A unit test in the collector module proves `tag_names` count maps to `release_tag_count` — assert against a synthetic count, OR a `#[test]` that constructs `HistoryFacts` with `release_tag_count: 7` and reads it back == 7 (no git fixture needed for the field mapping; the git2 call itself is exercised by the existing integration path).
    - `release_tag_count` appears in src/snapshot.rs and is set (not defaulted-away) in src/app/collect.rs.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib analyze::commit && cargo build --lib</automated>
  </verify>
  <done>HistoryFacts carries release_tag_count, populated via git2 tag_names() in collect(), crate compiles, mapping test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vibe classifier per VIBES.md (D-02, NARR-01)</name>
  <read_first>
    - .planning/research/VIBES.md (THE spec — read entirely: 7 vibes + exact weights, MIN_SCORE=4, scoring algorithm, specificity tie-break order, Chaotic Good fallback, evidence templates, Output Contract VibeResult shape, runner_up rule: 2nd-highest distinct vibe with score >= 2)
    - .planning/phases/05-polish-calibration/05-CONTEXT.md (D-02 — bilingual vibe names + bilingual evidence bullets; D-08 voice)
    - src/snapshot.rs + src/report/sections.rs (signal sources: HistoryFacts night_pct/weekend_pct/business_hours_pct/bus_factor/contributor_count/top_author_share_pct/commits_this_month/repo_age_days/release_tag_count; FirstImpressions.last_activity_secs for days-since-last-commit; BranchJungle.total + .stale; InfrastructureFootprints flags + count + dependabot/renovate for has_bot)
    - src/i18n.rs (bi/two_line/inline_label — bilingual helpers)
    - .planning/research/PITFALLS.md (Pitfall 3 — every bullet = a condition that fired)
  </read_first>
  <files>src/analyze/vibes.rs, src/analyze/mod.rs</files>
  <behavior>
    - A fixture with top_author_share_pct=70, contributor_count=4, bus_factor=1 => primary == Solo Wizard (score 3+2+2=7), evidence contains the three satisfied bullets only.
    - A fixture with age_years>=5 (repo_age_days>=1825) + days_since_last_commit>120 + release_tag_count>=5 => primary == Ancient Temple.
    - A fixture with all-zero / weak signals (no vibe reaches MIN_SCORE=4) => primary == Chaotic Good (fallback).
    - A fixture matching ALL Startup Goblin conditions PLUS night_pct>=25 + branch_count>=25 => primary == Sleep-Deprived Startup (superset outscores Goblin) — confirms specificity ordering.
    - On a score tie, the more-specific vibe wins per the specificity order (Sleep-Deprived Startup -> Solo Wizard -> Startup Goblin -> Corporate Fortress -> Open Source Kingdom -> Ancient Temple -> Chaotic Good).
    - runner_up == 2nd-highest DISTINCT vibe with score >= 2, else None; runner_up never equals primary.
    - evidence bullets are bilingual (each contains a VI substring and an EN substring) and never contain "mình".
  </behavior>
  <action>
    Create src/analyze/vibes.rs and register `pub mod vibes;` in src/analyze/mod.rs. Define `pub enum VibeLabel` with the 7 variants (SoloWizard, AncientTemple, CorporateFortress, OpenSourceKingdom, StartupGoblin, SleepDeprivedStartup, ChaoticGood) and a method returning its bilingual display name keeping the iconic EN + emoji AND a VI gloss per D-02 (e.g. `🧙 Solo Wizard / Phù thủy đơn độc`, `🏛️ Ancient Temple / Đền cổ`, `🏢 Corporate Fortress / Pháo đài công sở`, `👑 Open Source Kingdom / Vương quốc mã nguồn mở`, `🧌 Startup Goblin / Yêu tinh khởi nghiệp`, `😵 Sleep-Deprived Startup / Khởi nghiệp thiếu ngủ`, `🔥 Chaotic Good / Hỗn loạn nhưng tốt bụng`). Define `pub const MIN_SCORE: u32 = 4;`. Define `pub struct VibeResult { pub primary: VibeLabel, pub primary_score: u32, pub evidence: Vec<crate::i18n::Bilingual>, pub runner_up: Option<VibeLabel> }`. Implement `pub fn classify_vibes(snapshot: &InvestigationSnapshot, sections: &FactualSections) -> VibeResult` (pure; reads snapshot/FactualSections only). Implement the weighted-scoring algorithm from VIBES.md EXACTLY: for each vibe evaluate its conditions with the listed weights, collect a satisfied-condition evidence bullet for each (each bullet is a `bi(vi, en)` matching the spec's evidence template, with `{author}`/`{share}`/`{n}`/`{pct}`/`{days}`/`{age_years}` interpolated from the signals). Sleep-Deprived Startup shares ALL Startup Goblin conditions plus its two crunch conditions (so it naturally outscores Goblin). Compute age_years = repo_age_days/365; days_since_last_commit = (now_secs - last_activity_secs)/86400 when last_activity present. Pick winner = max score; tie-break by the specificity order. If winner score < MIN_SCORE force ChaoticGood (still emit ChaoticGood's satisfied positive bullets as evidence). runner_up = 2nd-highest distinct vibe with score >= 2 excluding winner, else None. Return the VibeResult holding ONLY the primary's satisfied evidence bullets. Add the `#[test]` fixtures described in <behavior> in this file, building FactualSections + InvestigationSnapshot directly (reuse the test-snapshot construction pattern from src/report/sections.rs tests; pass a fixed `now_secs`). Do NOT inline finding/verdict logic here. No fenced code in the implementation prose.
  </action>
  <acceptance_criteria>
    - `cargo test --lib analyze::vibes` passes with the fixtures from <behavior>: Solo Wizard fixture asserts `primary == VibeLabel::SoloWizard` AND evidence has exactly the 3 fired bullets; Chaotic-Good fallback fixture asserts `primary == VibeLabel::ChaoticGood`; Sleep-Deprived superset fixture asserts `primary == VibeLabel::SleepDeprivedStartup`; a tie fixture asserts specificity ordering; a runner_up fixture asserts `runner_up.is_some()` and `runner_up != Some(primary)`.
    - Source assertion: at least one evidence-bullet test asserts the rendered bullet contains both a VI substring and an EN substring, and the file/test contains no "mình".
    - `MIN_SCORE` == 4 and the 7 VibeLabel variants exist (grep-checkable).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib analyze::vibes</automated>
  </verify>
  <done>classify_vibes implements VIBES.md exactly (7 vibes, MIN_SCORE=4, tie-break, Chaotic Good fallback, bilingual evidence, runner_up), all fixtures green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Findings + Crab Verdict analyzers (D-03, D-04, NARR-02)</name>
  <read_first>
    - .planning/phases/05-polish-calibration/05-CONTEXT.md (D-03 finding rules + ranking + cap ~4-6 + runner-up line; D-04 strengths/risks lists + overall label; D-08 bilingual + Ferris)
    - .planning/research/PITFALLS.md (Pitfall 3 — grounded, playful but fair, never mean)
    - src/analyze/vibes.rs (from Task 2 — VibeLabel display name, VibeResult.runner_up consumed by findings)
    - src/report/sections.rs (FactualSections — signal inputs) + src/snapshot.rs
    - src/i18n.rs (bi/two_line/inline_label)
  </read_first>
  <files>src/analyze/findings.rs, src/analyze/verdict.rs, src/analyze/mod.rs</files>
  <behavior>
    - Findings: a fixture with bus_factor==1 fires a "bus factor 1" finding; missing CI fires a "no CI" finding; stale_branch_count high fires a stale-branch finding; very-high most-modified or single-dev dominance (top_author_share high) fires its finding; heavy night_pct fires a night-owl finding. Each finding is bilingual.
    - Findings are ranked by interestingness/severity and capped at <= 6 shown.
    - When VibeResult.runner_up.is_some(), exactly one finding line is the runner-up-vibe line ("Cũng phảng phất chất {runner_up} / Also gives off {runner_up} energy"); when runner_up is None, no such line.
    - Verdict: a healthy fixture (recently active, has_ci, bus_factor>=3, distributed authorship, has releases) => overall_label == Healthy ("Khỏe mạnh / Healthy"), strengths non-empty, risks light.
    - Verdict: a risky fixture (bus_factor==1, no CI, many stale branches, dormant) => risks non-empty and overall_label is the worst tier ("Bị ám / Haunted").
    - All strengths use a checkmark marker, all risks use a warning marker; every line traces to a fired signal (Pitfall 3).
  </behavior>
  <action>
    Create src/analyze/findings.rs and src/analyze/verdict.rs; register both in src/analyze/mod.rs. In findings.rs define `pub struct Finding { pub text: crate::i18n::Bilingual, pub weight: u32 }` and `pub fn interesting_findings(snapshot: &InvestigationSnapshot, sections: &FactualSections, vibe: &VibeResult) -> Vec<Finding>` (pure). Implement each finding as its own rule: it inspects a signal threshold and, when fired, pushes a bilingual `bi(vi, en)` observation with an interestingness `weight`. Cover at least: bus_factor==1, !has_ci, stale_branch_count >= a threshold, single-dev dominance (top_author_share_pct high), most-modified-file present/extreme, night_pct >= a threshold. After collecting, sort by weight descending and truncate to at most 6. If `vibe.runner_up` is Some, push the runner-up-vibe line (bilingual, using the runner-up's display name) — keep it within the cap (it counts toward the ~4-6). In verdict.rs define `pub struct CrabVerdict { pub strengths: Vec<crate::i18n::Bilingual>, pub risks: Vec<crate::i18n::Bilingual>, pub overall_label: crate::i18n::Bilingual }` and `pub fn crab_verdict(snapshot: &InvestigationSnapshot, sections: &FactualSections) -> CrabVerdict` (pure). Strengths fire on: recently active (days_since_last_commit small), has_ci, bus_factor >= 3, distributed authorship (top_author_share_pct low), release_tag_count > 0. Risks fire on: bus_factor==1, !has_ci, stale_branch_count high, dormant (days_since_last_commit large), single-dev dominance. Derive overall_label from the balance of strengths vs risks into three bilingual tiers: `bi("Khỏe mạnh","Healthy")`, `bi("Cần chăm sóc","Needs care")`, `bi("Bị ám","Haunted")` (you own the exact count thresholds per Discretion). Keep all copy playful but fair (Pitfall 3); Ferris narrates. Add the `#[test]` fixtures from <behavior> in each file (build snapshot/FactualSections/VibeResult directly). No fenced code in prose.
  </action>
  <acceptance_criteria>
    - `cargo test --lib analyze::findings` passes: bus_factor==1 fixture asserts a bus-factor finding present; runner_up=Some fixture asserts exactly one runner-up-vibe finding line; runner_up=None fixture asserts none; a >6-candidate fixture asserts `interesting_findings(...).len() <= 6` and ordering is weight-descending.
    - `cargo test --lib analyze::verdict` passes: healthy fixture asserts `overall_label.en == "Healthy"` and non-empty strengths; risky fixture asserts `overall_label.en == "Haunted"` and non-empty risks.
    - Source/bilingual assertion: at least one findings test and one verdict test assert a line contains both a VI substring and an EN substring; tests/files contain no "mình".
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib analyze::findings && cargo test --lib analyze::verdict</automated>
  </verify>
  <done>interesting_findings (ranked, capped, runner-up line) and crab_verdict (strengths/risks + 3-tier overall label) are pure, bilingual, grounded, fixtures green.</done>
</task>

<task type="auto">
  <name>Task 4: Render Sections 7-9 in report.rs + plain.rs and UPDATE the negative assert (D-06)</name>
  <read_first>
    - .planning/phases/05-polish-calibration/05-CONTEXT.md (D-06 — render via Section abstraction, after section 6, fixed order 1->9, bilingual+Ferris, UPDATE the negative TestBackend assert)
    - src/tui/section.rs (Section { title, body }.into_lines() — how 1-6 are emitted)
    - src/tui/report.rs (build_report_lines — append after Section 6 block ends at line ~294; signature `(session, sections, now_secs)`; the existing negative assert at lines 440-448 asserting "Vibes"/"Crab Verdict"/"Findings" ABSENT; the bilingual narrative + styling pattern of sections 1-6; the section-title monotonic-order check expecting `found_titles == 6`)
    - src/tui/plain.rs (render<W: Write> — append after Section 6 block ends at line ~138; the test asserting section titles present)
    - src/analyze/vibes.rs + findings.rs + verdict.rs (Task 2/3 outputs: classify_vibes / interesting_findings / crab_verdict; VibeResult/Finding/CrabVerdict shapes)
    - src/i18n.rs (bi/two_line/inline_label)
  </read_first>
  <files>src/tui/report.rs, src/tui/plain.rs</files>
  <action>
    In build_report_lines (src/tui/report.rs), AFTER the Section 6 block, call `classify_vibes(&snapshot-or-sections...)`, `interesting_findings(...)`, `crab_verdict(...)`. NOTE the analyzers take `&InvestigationSnapshot` and/or `&FactualSections`; build_report_lines currently receives only `&FactualSections` — extend its signature (and plain::render's) to also take `&InvestigationSnapshot` (or precompute the three results in app::run and pass them in — choose the lower-churn option, but keep the analyze->view-model->render separation). Render three sections via the `Section` abstraction exactly like 1-6 (bilingual title VI line + indented EN line + Ferris italic narrative two lines + spacer): Section 7 `REPOSITORY VIBES / VIBE REPOSITORY` (use a VI title e.g. "🔮 KHÍ CHẤT REPOSITORY" + EN "🔮 REPOSITORY VIBES") showing the primary vibe display name + each evidence bullet bilingual; Section 8 `Interesting Findings` (VI "🔎 PHÁT HIỆN THÚ VỊ" + EN "🔎 INTERESTING FINDINGS") listing each Finding bilingual (including the runner-up line); Section 9 `Crab Verdict` (VI "🦀 PHÁN QUYẾT CỦA FERRIS" + EN "🦀 CRAB VERDICT") with a Ferris lead-in ("Theo Ferris... / In Ferris's opinion..."), Strengths block (✓ lines), Risks block (⚠ lines), and the one-line overall_label. Mirror the SAME three sections in src/tui/plain.rs with writeln! in identical order/content (no styling). Then UPDATE the report.rs test: change the negative assert block (lines ~440-448) so it now asserts the Section 7/8/9 English titles ARE PRESENT (e.g. buffer contains "REPOSITORY VIBES", "INTERESTING FINDINGS", "CRAB VERDICT"), extend the `titles` monotonic-order vec to include the three new VI titles and bump the expected `found_titles` count from 6 to 9, and add at least one new bilingual + no-"mình" assertion for the new sections. UPDATE the plain.rs test to assert the three new section titles present and still no "mình"/no ANSI. Make the test fixtures construct a snapshot whose signals deterministically produce a known primary vibe so the title assertions are stable. No fenced code in prose.
  </action>
  <acceptance_criteria>
    - `cargo test --lib tui::report` passes: TestBackend buffer contains "REPOSITORY VIBES", "INTERESTING FINDINGS", "CRAB VERDICT"; the monotonic title-order check now passes with `found_titles == 9`; the negative assert no longer asserts absence (it now asserts presence); no "mình" in buffer.
    - `cargo test --lib tui::plain` passes: plain output contains the three new English section titles, contains no "\x1b[" and no "mình".
    - Source assertion: sections 7-9 emitted via `Section { ... }.into_lines()` in report.rs (grep `Section {` count >= 9 across the section blocks) and rendered after section 6 in both files.
    - `cargo test --lib` (whole crate) passes — all prior factual-section tests still green.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib tui::report && cargo test --lib tui::plain && cargo test --lib</automated>
  </verify>
  <done>Sections 7-9 render via the Section abstraction after the 6 factual sections in both renderers, bilingual+Ferris; the Phase 4 negative assert is updated to require presence; full crate test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git clone -> analyzers | Only locally-cloned repo data crosses in (no network in this phase); tag_names() reads local git objects |
| snapshot/FactualSections -> narrative analyzers | Pure functions over already-validated, in-memory data; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | release_tag_count via tag_names() | accept | tag_names is a cheap local enumeration with no network and no full-history walk (Pitfall 1 bound respected); thousands of tags is still O(n) cheap |
| T-05-02 | Information disclosure | findings/verdict copy | mitigate | Pitfall 3 — every line traces to a fired signal, playful but fair, never mean; no external data exfiltration (read-only, local) |
| T-05-03 | Tampering | empty/None signals (no commits, no branches, no last_activity) | mitigate | analyzers must not panic on empty/None: use saturating/checked arithmetic for days_since_last_commit and age_years, guard division, handle Option signals; Chaotic Good fallback covers the all-zero case; add an empty-snapshot fixture test |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies added this phase (tag_names is in the already-vendored git2); no package-install tasks, no legitimacy gate required |
</threat_model>

<verification>
- `cargo test --lib` passes (whole crate, all phases) with sections 7-9 present.
- `cargo build` succeeds; no new external dependency added.
- Vibe classifier matches VIBES.md exactly (7 vibes, MIN_SCORE=4, specificity tie-break, Chaotic Good fallback, evidence = fired conditions, runner_up -> Section 8).
- All narrative text bilingual (VI + EN), Ferris voice, no "mình"; grounded per Pitfall 3.
- No panics on empty/None signals (empty-snapshot fixture test green).
</verification>

<success_criteria>
- NARR-01: Repository Vibes classifies from signals and shows evidence bullets that justify the label (Solo Wizard / Chaotic Good fallback fixtures green).
- NARR-02: Interesting Findings (ranked, capped, runner-up line) + Crab Verdict (strengths/risks + overall label) derived from computed evidence (fixtures green).
- Sections 7-9 render via the Section abstraction after the 6 factual sections in both report.rs and plain.rs; Phase 4 negative assert updated to require presence.
- release_tag_count enriched onto the snapshot via git2 tag_names().
</success_criteria>

<artifacts_this_phase_produces>
- `release_tag_count: usize` field on `HistoryFacts` (src/snapshot.rs), populated in src/app/collect.rs via a new `release_tag_count(repo)` collector calling git2 `tag_names()`.
- `src/analyze/vibes.rs`: `VibeLabel` (7 variants + bilingual display), `VibeResult { primary, primary_score, evidence, runner_up }`, `MIN_SCORE: u32 = 4`, `classify_vibes(snapshot, sections) -> VibeResult`.
- `src/analyze/findings.rs`: `Finding { text, weight }`, `interesting_findings(snapshot, sections, vibe) -> Vec<Finding>` (ranked, capped ~4-6, runner-up line).
- `src/analyze/verdict.rs`: `CrabVerdict { strengths, risks, overall_label }`, `crab_verdict(snapshot, sections) -> CrabVerdict`.
- Sections 7 (Repository Vibes), 8 (Interesting Findings), 9 (Crab Verdict) rendered in src/tui/report.rs and src/tui/plain.rs via the `Section` abstraction; Phase 4 negative 7-9 assert UPDATED to assert presence.
- Any VIBES threshold constants introduced (e.g. MIN_SCORE) live in src/analyze/vibes.rs for 05-02 to tune.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-polish-calibration/05-01-SUMMARY.md` when done.
</output>
