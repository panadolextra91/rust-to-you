---
phase: 05-polish-calibration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/analyze/vibes.rs
  - src/analyze/findings.rs
  - src/analyze/verdict.rs
  - .planning/research/VIBES.md
  - .planning/phases/05-polish-calibration/CALIBRATION.md
autonomous: false
requirements: [NARR-01, NARR-02]
must_haves:
  truths:
    - "Every vibe, finding, and verdict rule has a deterministic unit test against a synthetic fixture (no network) (D-05, NARR-01, NARR-02)"
    - "A documented manual spot-check ran against ~3-4 diverse real repos (solo, large OSS like tokio-rs/tokio, dormant old, corporate-style) confirming labels feel right (D-05)"
    - "If a vibe category dominated, V1 thresholds were tuned by judgment AND the change was written back into research/VIBES.md (D-05)"
    - "End-to-end runs produce stable, trustworthy reports for representative public repos (ROADMAP SC3)"
  artifacts:
    - path: ".planning/phases/05-polish-calibration/CALIBRATION.md"
      provides: "Manual real-repo spot-check checklist + observed vibe/findings/verdict per repo + any threshold-change decisions"
      contains: "Spot-check"
    - path: "src/analyze/vibes.rs"
      provides: "Expanded deterministic fixture coverage for all 7 vibes + tie-break + fallback; any tuned thresholds"
      contains: "MIN_SCORE"
    - path: ".planning/research/VIBES.md"
      provides: "Threshold values reconciled with any Phase 5 tuning (calibration note updated)"
      contains: "MIN_SCORE = 4"
  key_links:
    - from: ".planning/phases/05-polish-calibration/CALIBRATION.md"
      to: ".planning/research/VIBES.md"
      via: "threshold changes recorded back into the spec"
      pattern: "VIBES.md"
    - from: "src/analyze/vibes.rs tests"
      to: "all 7 VibeLabel variants"
      via: "one deterministic fixture per vibe + tie-break + fallback"
      pattern: "VibeLabel::"
---

<objective>
Calibrate and end-to-end verify the narrative layer built in 05-01. Add deterministic unit-test coverage for EVERY vibe/finding/verdict rule against synthetic fixtures, run a manual spot-check against ~3-4 diverse real public repos, tune any V1 VIBES thresholds by judgment if a category dominates, and write any threshold change back into research/VIBES.md (the spec stays the source of truth).

Purpose: Satisfy ROADMAP Phase 5 success criteria — vibes always show justifying evidence (NARR-01), findings/verdict read coherently on real repos (NARR-02), and end-to-end runs are stable and trustworthy (SC3). This closes the loop: the V1 thresholds in VIBES.md were explicitly "starting guesses to tune in Phase 5".

Output: Expanded fixture test suite; CALIBRATION.md spot-check record; reconciled research/VIBES.md.
</objective>

<execution_context>
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-calibration/05-CONTEXT.md
@.planning/research/VIBES.md
@.planning/research/PITFALLS.md
@.planning/phases/05-polish-calibration/05-01-SUMMARY.md
@src/analyze/vibes.rs
@src/analyze/findings.rs
@src/analyze/verdict.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic rule coverage for all 7 vibes, findings, and verdict (D-05)</name>
  <read_first>
    - .planning/phases/05-polish-calibration/05-CONTEXT.md (D-05 — deterministic unit tests for EVERY vibe/finding/verdict rule against synthetic fixtures)
    - .planning/research/VIBES.md (the 7 vibes + exact weights + specificity tie-break + Chaotic Good fallback + MIN_SCORE=4 — the assertions must match the spec)
    - src/analyze/vibes.rs (classify_vibes, VibeLabel variants, VibeResult, MIN_SCORE — from 05-01; existing fixtures to extend)
    - src/analyze/findings.rs + src/analyze/verdict.rs (interesting_findings, crab_verdict — from 05-01)
    - src/report/sections.rs tests (the make_test_snapshot construction pattern to reuse for fixtures)
  </read_first>
  <files>src/analyze/vibes.rs, src/analyze/findings.rs, src/analyze/verdict.rs</files>
  <action>
    Extend the `#[test]` modules in vibes.rs, findings.rs, verdict.rs so coverage is complete and deterministic (no network, synthetic snapshot/FactualSections built directly with a fixed now_secs). In vibes.rs ensure there is at least ONE fixture per VibeLabel variant that asserts `primary == VibeLabel::X` (Solo Wizard, Ancient Temple, Corporate Fortress, Open Source Kingdom, Startup Goblin, Sleep-Deprived Startup, Chaotic Good), PLUS: a tie fixture asserting the specificity tie-break order, a fallback fixture (no vibe >= MIN_SCORE) asserting Chaotic Good, a runner_up fixture asserting `runner_up.is_some()` and `runner_up != Some(primary)` with score >= 2, and an EMPTY/None-signal fixture asserting no panic (last_activity absent, zero branches/commits) — guards T-05-03. For each vibe fixture also assert the evidence bullets equal exactly the conditions that fired per VIBES.md (count + that each bullet is bilingual VI+EN, no "mình"). In findings.rs add one fixture per finding rule asserting it fires on its threshold and stays silent below it, plus the cap (`len() <= 6`), weight-descending order, and runner-up-line presence/absence. In verdict.rs add fixtures covering each strength rule, each risk rule, and all three overall_label tiers (Healthy / Needs care / Haunted), asserting the bilingual EN substring of overall_label. Do NOT add network or filesystem dependencies. No fenced code in prose.
  </action>
  <acceptance_criteria>
    - `cargo test --lib analyze::vibes` passes with >= 7 per-variant assertions (one per VibeLabel) + tie-break + fallback + runner_up + empty-signal (no-panic) fixtures.
    - `cargo test --lib analyze::findings` passes covering each finding rule (fires-above / silent-below), cap <= 6, weight ordering, runner-up line.
    - `cargo test --lib analyze::verdict` passes covering each strength/risk rule and all three overall_label tiers.
    - Source assertion: each VibeLabel variant name appears in at least one test assertion in vibes.rs (grep `VibeLabel::` across the test module shows all 7); tests contain no "mình".
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib analyze::vibes && cargo test --lib analyze::findings && cargo test --lib analyze::verdict</automated>
  </verify>
  <done>Every vibe (7), finding, and verdict rule has a deterministic synthetic-fixture test including tie-break, fallback, runner_up, all verdict tiers, and a no-panic empty-signal case.</done>
</task>

<task type="auto">
  <name>Task 2: Manual real-repo spot-check + threshold reconciliation (D-05)</name>
  <read_first>
    - .planning/phases/05-polish-calibration/05-CONTEXT.md (D-05 — manual spot-check on ~3-4 diverse real repos; tune V1 thresholds by judgment if a category dominates; record threshold changes back into VIBES.md)
    - .planning/research/VIBES.md (Calibration Notes section — "if many real repos land on Chaotic Good, MIN_SCORE or thresholds are too strict"; the V1 threshold values)
    - .planning/research/PITFALLS.md (Pitfall 3 — labels must feel grounded and fair)
    - src/analyze/vibes.rs (thresholds + MIN_SCORE live here; the consts to tune)
  </read_first>
  <files>.planning/phases/05-polish-calibration/CALIBRATION.md, src/analyze/vibes.rs, .planning/research/VIBES.md</files>
  <action>
    Run the built binary against ~3-4 diverse real public repos chosen to cover the spectrum: a solo side-project, a large OSS project (tokio-rs/tokio), a dormant old repo, and a corporate-style repo. For each, run `cargo run -- <repo-url>` (or the plain renderer if not a TTY) and capture the Section 7 primary vibe + Section 8 findings + Section 9 verdict. Record results in a NEW file `.planning/phases/05-polish-calibration/CALIBRATION.md` with a per-repo table (repo, expected feel, observed vibe, observed findings/verdict, "feels right?" yes/no/note). Evaluate against VIBES.md Calibration Notes: if one category dominates (e.g. everything lands on Chaotic Good, or every active repo reads Corporate Fortress), tune the relevant V1 threshold consts in src/analyze/vibes.rs by judgment (keep labels grounded + fair per Pitfall 3). If ANY threshold or MIN_SCORE is changed, (a) update the corresponding value/table in research/VIBES.md and its Calibration Notes so the spec matches the code, and (b) update or add the deterministic fixture in 05-02 Task 1 that pins the changed boundary so the test suite reflects the new threshold. If no change is needed, record in CALIBRATION.md that V1 thresholds were validated as-is. This task is largely manual (real network clones) but the OUTCOME — any code/spec change — is covered by the deterministic suite in Task 1; the binary runs themselves are manual-only. No fenced code in prose.
  </action>
  <acceptance_criteria>
    - `.planning/phases/05-polish-calibration/CALIBRATION.md` exists with a spot-check table covering >= 3 real repos (solo / large OSS / dormant / corporate-style), each with observed vibe + findings + verdict + a feels-right verdict.
    - If any threshold changed: the same numeric value appears in BOTH src/analyze/vibes.rs and research/VIBES.md (no drift), and a deterministic fixture pins the new boundary (`cargo test --lib analyze::vibes` still green).
    - If no threshold changed: CALIBRATION.md explicitly records "V1 thresholds validated, no change".
    - `cargo test --lib` (whole crate) passes after any tuning (deterministic suite reflects final thresholds).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --lib</automated>
    <human-check>CALIBRATION.md spot-check table reviewed: the 3-4 real-repo labels feel grounded and fair (Pitfall 3); any VIBES.md threshold change is reflected identically in src/analyze/vibes.rs.</human-check>
  </verify>
  <done>Manual spot-check on ~3-4 diverse real repos recorded in CALIBRATION.md; thresholds tuned-by-judgment if a category dominated, with changes written back into VIBES.md and pinned by a deterministic fixture; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Calibration review checkpoint (D-05)</name>
  <action>PAUSE for human verification — do not auto-advance. Present the cargo test result and CALIBRATION.md spot-check table; wait for the reviewer to confirm the real-repo labels feel grounded and fair (Pitfall 3) and that any VIBES.md threshold change matches src/analyze/vibes.rs before marking the phase done.</action>
  <what-built>Deterministic fixture coverage for all 7 vibes + findings + verdict (05-02 Task 1), and a manual real-repo spot-check (05-02 Task 2) recorded in CALIBRATION.md, with any VIBES thresholds tuned and reconciled into research/VIBES.md.</what-built>
  <how-to-verify>
    1. Run `cargo test --lib` — confirm all tests pass (every vibe/finding/verdict rule covered).
    2. Open `.planning/phases/05-polish-calibration/CALIBRATION.md` — confirm the spot-check table covers a solo repo, tokio-rs/tokio (large OSS), a dormant old repo, and a corporate-style repo, each with an observed vibe/findings/verdict and a "feels right?" note.
    3. Optionally re-run `cargo run -- <one of the spot-check repo URLs>` and eyeball Sections 7-9: vibe label has justifying evidence bullets, findings are grounded (each traces to a real signal), verdict strengths/risks + overall label read coherently and fairly (Pitfall 3).
    4. If any threshold was changed, confirm the value matches between src/analyze/vibes.rs and research/VIBES.md.
  </how-to-verify>
  <resume-signal>Type "approved" if the labels feel right and the spec/code/tests agree, or describe what feels off (which repo, which label) so thresholds can be re-tuned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real GitHub repos -> binary (manual spot-check only) | Real clones happen only in the manual Task 2 runs; analyzers stay pure over the resulting snapshot; read-only, no write actions |
| deterministic fixtures -> analyzers | Synthetic in-memory data; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | threshold drift between code and spec | mitigate | Any tuned threshold must be identical in src/analyze/vibes.rs and research/VIBES.md AND pinned by a deterministic fixture; acceptance criteria enforce no-drift |
| T-05-05 | Information disclosure | manual spot-check copy on real repos | mitigate | Pitfall 3 — confirm real-repo labels are grounded and fair, never mean; human-verify checkpoint reviews tone |
| T-05-03 | Tampering | empty/None signals on real dormant repo | accept | already covered by the no-panic empty-signal fixture in Task 1; dormant-repo spot-check exercises it live |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; no install tasks; no legitimacy gate required |
</threat_model>

<verification>
- `cargo test --lib` passes with one deterministic fixture per vibe (7), per finding rule, per verdict rule + tie-break + fallback + runner_up + empty-signal no-panic.
- CALIBRATION.md records a manual spot-check on ~3-4 diverse real repos with feels-right judgments.
- Any tuned threshold appears identically in src/analyze/vibes.rs and research/VIBES.md and is pinned by a fixture (no drift).
- Real-repo reports read coherently and fairly (Pitfall 3) — confirmed at the human-verify checkpoint.
</verification>

<success_criteria>
- NARR-01: Repository Vibes always show justifying evidence bullets — confirmed by per-vibe fixtures and the real-repo spot-check.
- NARR-02: Interesting Findings + Crab Verdict read coherently on real sample repos — confirmed in CALIBRATION.md and the checkpoint.
- End-to-end runs produce stable, trustworthy reports for representative public repos (ROADMAP SC3).
- VIBES.md remains the reconciled source of truth for all thresholds.
</success_criteria>

<artifacts_this_phase_produces>
- Expanded deterministic test coverage in src/analyze/vibes.rs, findings.rs, verdict.rs (one fixture per vibe/finding/verdict rule + tie-break + fallback + runner_up + empty-signal no-panic).
- `.planning/phases/05-polish-calibration/CALIBRATION.md`: real-repo spot-check table (solo / tokio-rs/tokio / dormant / corporate-style) + any threshold-change decisions.
- Reconciled `.planning/research/VIBES.md` (thresholds/Calibration Notes updated to match any Phase 5 tuning).
- Any tuned threshold consts in src/analyze/vibes.rs (kept identical to VIBES.md).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-polish-calibration/05-02-SUMMARY.md` when done.
</output>
