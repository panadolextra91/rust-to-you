---
phase: 06-safe-intake-pre-flight-guard
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - docs/THREAT-MODEL.md
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "docs/THREAT-MODEL.md exists and documents the full intake attack surface"
    - "The doc covers leading-dash arg-injection, .. traversal, backslash, control chars, and oversize-clone DoS"
    - "The doc explicitly states git2 is libgit2 via FFI with no shell / no git subprocess spawn"
    - "Each documented threat names the test (in src/cli/parse.rs from 06-01) or guard that backs it"
  artifacts:
    - path: "docs/THREAT-MODEL.md"
      provides: "Version-controlled intake threat model (STRIDE table + no-shell rationale + test backing)"
      contains: "libgit2"
      min_lines: 30
  key_links:
    - from: "docs/THREAT-MODEL.md"
      to: "src/cli/parse.rs::test_parse_repo_ref_reject"
      via: "names the reject-table rows as backing evidence"
      pattern: "parse_repo_ref"
    - from: "docs/THREAT-MODEL.md"
      to: "src/app/collect.rs size guard"
      via: "references the pre-flight 500 MB DoS mitigation"
      pattern: "500 MB"
---

<objective>
Document the intake threat model in a standalone, version-controlled `docs/THREAT-MODEL.md` (D-15),
backed by the injection/abuse tests that already prove rejection happens at the parser before any
git2 / network call (D-16, those tests ship in 06-01's `parse.rs` reject table). This completes
SEC-02: "the intake threat model is documented and covered by explicit injection/abuse tests."

Purpose: Make the security posture of the intake surface explicit and auditable — what is rejected,
where, and why the injection surface is narrow by construction (`git2` is libgit2 FFI, no shell spawn).

Output: `docs/THREAT-MODEL.md` (the `docs/` directory is greenfield — created here).
</objective>

<execution_context>
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huynhngocanhthu/rust-to-you/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-safe-intake-pre-flight-guard/06-CONTEXT.md
@.planning/phases/06-safe-intake-pre-flight-guard/06-RESEARCH.md
@.planning/phases/06-safe-intake-pre-flight-guard/06-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (06-03) creates:

- `docs/THREAT-MODEL.md` — the version-controlled intake threat model (D-15), the documentation
  half of SEC-02. Its backing tests (the injection/abuse reject table) are produced by 06-01 in
  `src/cli/parse.rs`; this doc references them but does not add code or tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/THREAT-MODEL.md covering the intake attack surface with test/guard backing</name>
  <files>docs/THREAT-MODEL.md</files>
  <read_first>
    - .planning/phases/06-safe-intake-pre-flight-guard/06-RESEARCH.md (§Security Domain — the STRIDE threat table at RESEARCH.md:457-471 is the content spec; the no-shell-spawn verification is RESEARCH.md:472 and Sources)
    - .planning/phases/06-safe-intake-pre-flight-guard/06-PATTERNS.md (§docs/THREAT-MODEL.md — greenfield, use RESEARCH §Security Domain as the content spec)
    - .planning/phases/06-safe-intake-pre-flight-guard/06-CONTEXT.md (D-15, D-16)
    - src/cli/parse.rs (the guards + reject table this doc must cite — confirm the variant names and rows shipped by 06-01)
    - src/app/collect.rs (the 500 MB size guard this doc must cite for the DoS mitigation)
  </read_first>
  <action>
    Create the `docs/` directory (greenfield — does not exist yet) and write `docs/THREAT-MODEL.md`.
    Structure it as a STRIDE table sourced from RESEARCH §Security Domain (RESEARCH.md:457-471), with
    one row per intake threat: (1) argument injection via leading `-` (e.g. `--upload-pack`) →
    Tampering/EoP, mitigated by the parse_repo_ref leading-dash reject (→ `UnsafeInput`, D-10);
    (2) path traversal `..` → Tampering, mitigated by the existing `..` reject; (3) backslash `\` →
    Tampering, existing reject; (4) control chars / over-length input → Tampering/DoS, existing
    control-char + 2048 cap plus the new owner ≤ 39 / repo ≤ 100 caps (D-11); (5) unsupported host /
    SSRF-ish → Spoofing, host allowlist; (6) oversize-clone DoS (machine hang) → Denial of Service,
    mitigated by the pre-flight `size > 500 MB` refuse-by-default with the `--deep` warned opt-in
    (GUARD-01). For each threat, name the backing guard AND the test that proves it (the
    `test_parse_repo_ref_reject` rows in `src/cli/parse.rs` for the parser threats — D-16). Include a
    clearly-stated structural section: **`git2` is libgit2 bound via FFI — rust-to-you spawns NO
    shell and NO `git` subprocess** (verified: zero `std::process::Command` in `src/`), which is what
    makes the classic flag-injection vector absent by construction and the injection surface narrow.
    Note the tool is read-only and public-GitHub-only. Keep it factual and concise (this is a "tighten
    + document" phase — no speculative threats, no new subsystem). Any new prose Ferris-voiced lines
    are not required here (this is a developer-facing doc, English/technical is fine).
  </action>
  <verify>
    <automated>test -f docs/THREAT-MODEL.md && grep -q "libgit2" docs/THREAT-MODEL.md && grep -q "500 MB" docs/THREAT-MODEL.md && grep -q "parse_repo_ref" docs/THREAT-MODEL.md && grep -qi "no shell" docs/THREAT-MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/THREAT-MODEL.md` exists
    - contains "libgit2" AND a "no shell" / "no `git` subprocess" statement
    - contains "500 MB" (oversize-clone DoS mitigation) and references the `--deep` opt-in
    - references `parse_repo_ref` and its reject-table tests as backing evidence (D-16)
    - covers all five intake threats: leading `-`, `..`, `\`, control/over-length, oversize DoS
    - file is at least 30 lines
  </acceptance_criteria>
  <done>docs/THREAT-MODEL.md documents the full intake attack surface, names the backing guards/tests for each threat, and states the no-shell-spawn structural fact — completing SEC-02's documentation half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (documentation plan) | This plan adds no code path; it documents the boundaries enforced by 06-01 (parser) and 06-02 (size guard). The doc itself is the deliverable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Repudiation / audit gap | undocumented intake surface | mitigate | docs/THREAT-MODEL.md makes the intake threat model explicit and auditable (SEC-02, D-15), each threat tied to its guard + test |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (docs-only change) — nothing to slopcheck |
</threat_model>

<verification>
- `docs/THREAT-MODEL.md` exists, mentions libgit2/no-shell, 500 MB DoS mitigation, and the parser guards + tests
- The threats documented match the guards shipped in 06-01 (parser) and 06-02 (size guard)
</verification>

<success_criteria>
- SEC-02 (documentation): the intake threat model is documented in a version-controlled file
- The doc covers leading-`-`, `..`, `\`, control/over-length, and oversize-clone DoS
- The doc states git2 is libgit2 FFI with no shell / no git subprocess spawn
- Each threat is backed by a named guard and (for parser threats) a named test
</success_criteria>

<output>
Create `.planning/phases/06-safe-intake-pre-flight-guard/06-03-SUMMARY.md` when done.
</output>
